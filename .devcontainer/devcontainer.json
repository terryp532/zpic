{
  "name": "ZPIC Classroom",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.10-bookworm",
  "postCreateCommand": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends build-essential ffmpeg && python -m pip install --upgrade pip && python -m pip install -r binder/requirements.txt jupyterlab ipykernel && make -C python",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  },
  "forwardPorts": [
    8888
  ],
  "remoteUser": "vscode"
}
